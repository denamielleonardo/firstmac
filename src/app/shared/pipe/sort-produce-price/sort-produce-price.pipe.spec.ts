import { SortProducePricePipe } from './sort-produce-price.pipe';

describe('SortProducePricePipe', () => {
  it('create an instance', () => {
    const pipe = new SortProducePricePipe();
    expect(pipe).toBeTruthy();
  });
});
